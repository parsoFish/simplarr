PUID=${userId}
PGID=${userGroupId}
TZ=${timezone}
SUBDOMAIN_TLD=${subdomainTld}
DOCKER_CONFIG=${localPathToConfigDirectory}
NAS_IP=${nasIp}
NAS_IP_DASHED=${nasIpDashed}
PLEX_DIRECT_HASH=${plexDirectHash}
